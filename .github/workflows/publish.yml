name: 📦 Publish to PyPI

on:
  push:
    tags:
      - "v*" # Only publish on tag pushes starting with v (v1.0.0)

jobs:
  build:
    name: 📦 Build distribution
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"
          enable-cache: "auto"
          cache-dependency-glob: |
            **/pyproject.toml
            **/uv.lock
          github-token: ${{ github.token }}
          add-problem-matchers: "true"
          restore-cache: "true"
          save-cache: "true"

      - name: Extract version from tag and update _version.py
        id: tag_version
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          VERSION=${TAG_NAME#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Tag: $TAG_NAME -> Version: $VERSION"
          
          # Update _version.py with version from tag
          cat > src/loclean/_version.py << EOF
          """Version information for loclean package."""
          
          __version__ = "$VERSION"
          EOF
          
          echo "Updated src/loclean/_version.py with version: $VERSION"

      - name: Install build dependencies
        run: uv sync --dev --no-install-project

      - name: Build a binary wheel and a source tarball
        run: uv build

      - name: Verify built packages
        run: |
          ls -lh dist/
          echo "Built packages:"
          for file in dist/*; do
            echo "  - $(basename $file) ($(du -h $file | cut -f1))"
          done

      - name: Store the distribution packages
        uses: actions/upload-artifact@v6 # please don't change to older than this
        with:
          name: python-package-distributions
          path: dist/

  publish-to-pypi:
    name: 📦 Publish to PyPI
    if: startsWith(github.ref, 'refs/tags/') # only publish to PyPI on tag pushes
    needs:
      - build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: pypi
      url: https://pypi.org/p/loclean
    permissions:
      id-token: write # IMPORTANT: mandatory for trusted publishing
      contents: write # Required for creating GitHub releases

    steps:
      - name: Download all the dists
        uses: actions/download-artifact@v7 # please don't change to older than this
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          print-hash: true
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/*
          generate_release_notes: true
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          make_latest: true
          fail_on_unmatched_files: false